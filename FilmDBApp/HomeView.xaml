<UserControl x:Class="WpfApp1.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid HorizontalAlignment="Left" Height="800" VerticalAlignment="Top" Width="800" Background="White">
        <ListView HorizontalAlignment="Left" Height="309" Margin="10,10,0,0" VerticalAlignment="Top" Width="153" ItemsSource="{Binding ListOfGenres}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Genre" DisplayMemberBinding="{Binding GenreName}" Width="153"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
